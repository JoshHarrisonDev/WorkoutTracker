# WorkoutTracker
ASP.NET 5 MVC Application, Used to learn .NET 5

Functionality

Create a workout (CRUD)

Create an exercise for a workout (CRUD)

Create sets for an exercise (CRUD)

Uses Identity Framework to facilitate authentication

Uses MSSQL Server and Entity Framework to store Data
